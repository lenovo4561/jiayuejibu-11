<import name="privacy-modal" src="./PrivacyModal.ux"></import>

<template>
  <div class="splash-container">
    <image class="logo" src="/pkg_main/assets/img/logo.png"></image>

    <privacy-modal
      show="{{showPrivacy}}"
      onagree-evt="handleAgree"
    ></privacy-modal>
  </div>
</template>

<script>
import router from '@system.router'
import storage from '@system.storage'

export default {
  private: {
    showPrivacy: false
  },
  onInit() {
    // 检查是否已同意协议
    storage.get({
      key: 'agreed_privacy',
      success: data => {
        if (data === 'true') {
          // 已同意，直接进入首页
          this.goHome()
        } else {
          // 未同意，显示模态框
          this.showPrivacy = true
        }
      },
      fail: () => {
        // 第一次使用，显示模态框
        this.showPrivacy = true
      }
    })
  },
  handleAgree() {
    this.showPrivacy = false
    // 保存同意状态
    storage.set({
      key: 'agreed_privacy',
      value: 'true'
    })
    this.goHome()
  },
  goHome() {
    router.replace({
      uri: '/pkg_main/pages/Demo'
    })
  }
}
</script>

<style>
.splash-container {
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100%;
  background-image: url('/pkg_main/assets/img/loading-bg.png');
  background-size: cover;
  background-position: top center;
  padding-top: 0;
  margin-top: 0;
}

.logo {
  width: 46px;
  height: 46px;
  object-fit: contain;
}
</style>
